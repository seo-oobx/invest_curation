[build]
builder = "NIXPACKS"
buildCommand = "cd api && pip install -r requirements.txt"

[nixpacks]
plan = { providers = ["python"] }

[deploy]
startCommand = "cd api && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
